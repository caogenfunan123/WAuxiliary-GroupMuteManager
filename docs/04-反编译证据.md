# 04 · 反编译证据（类/方法清单 + 关键摘录）

全部来自微信 8.0.74 主 APK 的 classes*.dex。工具链见 `06-环境与工具.md`。
类名为混淆名，跨版本**必然变化**——换版本请按同样的锚点字符串重新定位。

## 关键类速查

| 类 | 身份 | 关键处 |
|---|---|---|
| `c01.e2` | ContactStorageLogic | `g0(z3,bool)`、`n(z3):r45.tn4`、`p0/z0(z3,bool)` |
| `mn.a` | oplog **cmd20** notify 任务 | ctor `(Ljava/lang/String;II)V`，extends `xg3.q0` |
| `xg3.q0` | oplog 任务基类 | `a()[B`、`b()I`、`j:Lprotobuf/f` |
| `xg3.p0` | modContact 任务（**cmd2，非正解**） | ctor `(I, Lcom/tencent/mm/protobuf/f;)V` |
| `e21.j` | NetSceneOplog | extends `m1`；ctor `(Ljava/util/List;)V`；static `H(List):r45.c50` |
| `e21.g` / `e21.h` / `e21.i` | MMReqRespBase / req / resp | `g` extends `k1` |
| `r45.h25` | oplog 请求包装 | 字段 `d:r45.c50`；`b()` 自建 `modelbase.i` |
| `r45.c50` | oplog 顶层 | `d:I count`、`e:LinkedList` |
| `r45.b50` | oplog 单 op | `d:I cmdId`、`e:r45.cu5` |
| `r45.cu5` | op 载荷 | `d:I len`、`e:Z`、`f:g buf`、`g:Z` |
| `r45.tn4` | ModContact proto | `.w = z3.T`（服务端通知位） |
| `r45.rn4` | **cmd20** proto | `d:du5 username`、`e:int g`、`f:int` |
| `modelbase.m1` | NetSceneBase | 队列要的就是这个 |
| `modelbase.k1` | MMReqRespBase | `e21.g` 的父类（≠ NetScene） |
| `modelbase.r1` | NetSceneQueue | `g(m1)=h(m1,0)`；"forbid in waiting: type=" |
| `modelbase.l` | CommReqResp builder | fields `a,b:protobuf/f`、`c:uri`、`d:cgi`；`a():modelbase.o` |
| `modelbase.z2` | **RunCgi dispatcher** | `d(o, e3, bool)` → `f(o, executor, cb, keep)`；"MicroMsg.RunCgi" |
| `gm0.j1` | MMKernel 入口 | `.d()` → queue；`.s(vg3.x3.class)` → ContactStorage 门面 |
| `vg3.x3` | ContactStorage 接口 | `.Bi()=存储`, `.fj()=任务队列` |
| `x2`（sdk.platformtools） | Application 持有者 | 静态字段取 Context（SP 用） |

## 决定性摘录

### 1) `RoomInfoDetailUI` 的极性（`g==0` = 静音开）
```java
this.g = z3VarN.T;                      // T 来自缓存 bean
if (this.g == 0) ... 静音图标可见 / checkbox 选中 / SP=true
// 点击 handler：this.g = 1 - this.g;  new a(this.p, this.g, 0);  z3.J2(g); Bi().p0(...)
```

### 2) setter 内部只推 cmd2（不够）
```java
static void g0(z3 v, boolean z) {
    h0(v);
    if (!z || v.m4(v.d1()) || v.p4(v.d1()) || v.J3(v.d1())) return;
    ((z0) j1.s(x3.class).fj()).c(new p0(2, n(v)));   // ← modContact，cmd 2
}
```

### 3) `n()` 里服务端通知位的来源
```java
tn4.w = z3Var.T;    // 服务端 mute 状态 = T
tn4.n = z3Var.getType();
```

### 4) cmd20 任务本体（`mn/a.smali`）
```smali
.method public constructor <init>(Ljava/lang/String;II)V
    const/16 v0, 0x14                 # super(20) → cmd 20
    invoke-direct {p0, v0}, Lxg3/q0;-><init>(I)V
    new-instance v0, Lr45/rn4;
    ... du5.d = username; rn4.e = arg2(g); rn4.f = arg3(0)
    iput-object v0, p0, Lxg3/q0;->j:Lcom/tencent/mm/protobuf/f;
```

### 5) `e21.j.H(List)`——q0 → c50 的自动组装
```smali
invoke-virtual {v2}, Lxg3/q0;->a()[B     # task 出字节
new-instance v4, Lr45/b50;
iput v2, v4, Lr45/b50;->d:I              # cmdId = task.b()
new-instance v2, Lr45/cu5;
cu5.d([B) → b50.e                       # 载荷
LinkedList.add(b50) → c50.e
```

### 6) `xg3.p0` 的真实 ctor（基类参数）
```smali
.method public constructor <init>(ILcom/tencent/mm/protobuf/f;)V
```

### 7) RunCgi 入口（没用对命令，留作层级证据）
```smali
method public static d(Lcom/tencent/mm/modelbase/o;Lcom/tencent/mm/modelbase/e3;Z)Lm1;
  → e(o, cb, keep) → f(o, executor, cb, false)
  f(): new modelbase.c3(o, executor, cb) → HandlerThread -> 网络派发
```

## 定位方法（换版本复用）

- Jadx 全局搜锚点字符串：
  - `"OpenImOpLogLogic OpenIMModContactMuteOplog username:%s switch add"` → `c01.e2.p0`
  - `"MicroMsg.ContactStorageLogic", "oplog modContact user:%s rank:%s BitVal:%d..."` → `c01.e2.n`
  - `"forbid in waiting: type="` → `modelbase.r1`（队列）
  - `"MicroMsg.RunCgi"` → `modelbase.z2`（RunCgi）
  - `"Kernel not null, has initialized."` → `gm0.j1`（MMKernel）
- baksmali 反 smali 后跟调用链：`r1.h ← r1.g ← 各 NetScene 构造`

## 本机反编译产物索引（未随仓库上传，体量过大）

| 目录 | 内容 |
|---|---|
| `wx11-src/sources/` | classes11 jadx 输出（`chatroom/ui/RoomInfoDetailUI.java`、`ui/chatting/ChattingUI.java` 所在） |
| `e2bad/sources/c01/e2.java` | `g0/n/p0/z0` 完整反编译 |
| `s4/` | classes4 baksmali（`mn/a.smali`、`xg3/q0.smali` 所在） |
| `s11/` | classes11 baksmali（`e21/j.smali`、`e21/h.smali`） |
| `s12/` | classes12 baksmali（`modelbase/*.smali`、`protobuf/g.smali`） |
| `s2/` | `xg3/p0.smali` 所在 |
| `c9bad/`、`s4/` | `r45/{h25,c50,b50,cu5}.smali` 所在 |
| `wemod/We源码/` | WeKit 参考实现（第三方，仅查证用） |
